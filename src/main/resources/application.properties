# Spring settings
server.port=8080

# Optional: PostgreSQL (used in Step 4)
spring.datasource.url=jdbc:postgresql://localhost:5432/securefilechain
spring.datasource.username=postgres
spring.datasource.password=your_password
spring.jpa.hibernate.ddl-auto=update
